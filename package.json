{
  "name": "vibe-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix server run build && npm --prefix web run build",
    "start": "concurrently \"npm --prefix server run start\" \"npm --prefix web run preview\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

